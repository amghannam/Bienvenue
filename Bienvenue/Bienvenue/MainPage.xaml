<Page
    x:Class="Bienvenue.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bienvenue"
    xmlns:views="using:Bienvenue.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding MainViewModel, Source={StaticResource ViewModelLocator}}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <ListView Grid.Row="1" ScrollViewer.VerticalScrollBarVisibility="Disabled" ScrollViewer.HorizontalScrollBarVisibility="Auto"
                ScrollViewer.HorizontalScrollMode="Enabled"
                ScrollViewer.VerticalScrollMode="Disabled"
                ItemsSource="{Binding CountryViewModels}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Background="Transparent" Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <views:CountryUserControl Width="400"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <CommandBar Background="RoyalBlue" Foreground="White" Grid.Row="2" IsOpen="True" IsSticky="True">
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Cortana" Foreground="White"/>
                <AppBarSeparator/>
                <AppBarButton Label="Compare" Foreground="White"/>
                <AppBarSeparator/>
                <AppBarButton Label="Reset" Foreground="White" Command="{Binding ResetCountries}"/>
                <AppBarButton Label="Add" Command="{Binding AddCountry}"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</Page>
