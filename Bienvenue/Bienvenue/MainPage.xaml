<Page
    x:Class="Bienvenue.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Bienvenue"
    xmlns:views="using:Bienvenue.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        <views:CountryUserControl/>
        <CommandBar Background="RoyalBlue" Foreground="White" Grid.Row="1" IsOpen="True" IsSticky="True">
            <CommandBar.PrimaryCommands>
                <AppBarButton Label="Compare" Foreground="White"/>
                <AppBarButton Label="Exit" Foreground="White"/>
                <AppBarButton Label="Cortana" Foreground="White"/>
            </CommandBar.PrimaryCommands>
        </CommandBar>
    </Grid>
</Page>
